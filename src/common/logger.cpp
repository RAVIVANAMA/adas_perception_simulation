// logger.cpp – the Logger is header-only (template + inline methods).
// This translation unit exists to satisfy CMake; it may be used for
// out-of-line definitions or explicit instantiations in the future.
#include "common/logger.hpp"
